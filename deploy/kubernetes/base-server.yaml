apiVersion: v1
kind: ConfigMap
metadata:
  name: kis-auto-trading-nginx
  namespace: default
data:
  default.conf.template: |
    map $http_upgrade $connection_upgrade {
      default upgrade;
      '' close;
    }

    server {
      listen 80;
      location / {
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Instance-Name $hostname;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        proxy_pass http://${UPSTREAM_HOST}:8000;
      }
    }
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: kis-auto-trading
  namespace: default
  labels:
    app.kubernetes.io/name: kis-auto-trading
    app.kubernetes.io/component: application
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: kis-auto-trading
      app.kubernetes.io/component: application
  template:
    metadata:
      labels:
        app.kubernetes.io/name: kis-auto-trading
        app.kubernetes.io/component: application
    spec:
      containers:
      - name: application
        image: kis-auto-trading:latest
        imagePullPolicy: IfNotPresent
        command: ["python", "-m", "uvicorn", "kis_auto_trading.application.compositions.signal_api:app", "--host", "0.0.0.0", "--port", "8000"]
        ports:
        - name: http
          containerPort: 8000
        env:
        - name: INSTANCE_ID
          valueFrom:
            fieldRef:
              fieldPath: metadata.name
        - name: LOG_DIRECTORY
          value: /app/logs
        - name: ACCOUNT_SHARD_1_DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: my-kis-gold-bar
              key: ACCOUNT_SHARD_1_DATABASE_URL
        - name: ACCOUNT_SHARD_2_DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: my-kis-gold-bar
              key: ACCOUNT_SHARD_2_DATABASE_URL
        - name: AUTOMATION_DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: my-kis-gold-bar
              key: AUTOMATION_DATABASE_URL
        - name: CONTROL_PLANE_API_TOKEN
          valueFrom:
            secretKeyRef:
              name: my-kis-gold-bar
              key: CONTROL_PLANE_API_TOKEN
        - name: CONTROL_PLANE_HEARTBEAT_URL
          valueFrom:
            secretKeyRef:
              name: my-kis-gold-bar
              key: CONTROL_PLANE_HEARTBEAT_URL
        - name: DURABLE_JOB_API_TOKEN
          valueFrom:
            secretKeyRef:
              name: my-kis-gold-bar
              key: DURABLE_JOB_API_TOKEN
        - name: IDENTITY_DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: my-kis-gold-bar
              key: IDENTITY_DATABASE_URL
        - name: KIS_ACCOUNT_ENVIRONMENT
          valueFrom:
            secretKeyRef:
              name: my-kis-gold-bar
              key: KIS_ACCOUNT_ENVIRONMENT
        - name: KIS_ACCOUNT_NUMBER
          valueFrom:
            secretKeyRef:
              name: my-kis-gold-bar
              key: KIS_ACCOUNT_NUMBER
        - name: KIS_ACCOUNT_PRODUCT_CODE
          valueFrom:
            secretKeyRef:
              name: my-kis-gold-bar
              key: KIS_ACCOUNT_PRODUCT_CODE
        - name: KIS_API_URL
          valueFrom:
            secretKeyRef:
              name: my-kis-gold-bar
              key: KIS_API_URL
        - name: KIS_APP_KEY
          valueFrom:
            secretKeyRef:
              name: my-kis-gold-bar
              key: KIS_APP_KEY
        - name: KIS_APP_SECRET
          valueFrom:
            secretKeyRef:
              name: my-kis-gold-bar
              key: KIS_APP_SECRET
        - name: KIS_TOKEN_SCOPE
          valueFrom:
            secretKeyRef:
              name: my-kis-gold-bar
              key: KIS_TOKEN_SCOPE
        - name: OPERATOR_API_TOKEN
          valueFrom:
            secretKeyRef:
              name: my-kis-gold-bar
              key: OPERATOR_API_TOKEN
        - name: RABBITMQ_URL
          valueFrom:
            secretKeyRef:
              name: my-kis-gold-bar
              key: RABBITMQ_URL
        - name: REDIS_URL
          valueFrom:
            secretKeyRef:
              name: my-kis-gold-bar
              key: REDIS_URL
        readinessProbe:
          httpGet:
            path: /readiness
            port: http
          initialDelaySeconds: 3
          periodSeconds: 5
        livenessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 10
          periodSeconds: 10
        volumeMounts:
        - name: execution-logs
          mountPath: /app/logs
      volumes:
      - name: execution-logs
        hostPath:
          path: /run/desktop/mnt/host/c/kis-auto-trading/logs
          type: DirectoryOrCreate
---
apiVersion: v1
kind: Service
metadata:
  name: kis-auto-trading-backend
  namespace: default
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: kis-auto-trading
    app.kubernetes.io/component: application
  ports:
  - name: http
    port: 8000
    targetPort: http
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: kis-auto-trading-durable-job-worker
  namespace: default
  labels:
    app.kubernetes.io/name: kis-auto-trading
    app.kubernetes.io/component: durable-job-worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: kis-auto-trading
      app.kubernetes.io/component: durable-job-worker
  template:
    metadata:
      labels:
        app.kubernetes.io/name: kis-auto-trading
        app.kubernetes.io/component: durable-job-worker
    spec:
      containers:
      - name: durable-job-worker
        image: kis-auto-trading:latest
        imagePullPolicy: IfNotPresent
        command: ["python", "scripts/run_durable_job_worker.py"]
        env:
        - name: INSTANCE_ID
          valueFrom:
            fieldRef:
              fieldPath: metadata.name
        - name: ACCOUNT_SHARD_1_DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: my-kis-gold-bar
              key: ACCOUNT_SHARD_1_DATABASE_URL
        - name: ACCOUNT_SHARD_2_DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: my-kis-gold-bar
              key: ACCOUNT_SHARD_2_DATABASE_URL
        - name: AUTOMATION_DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: my-kis-gold-bar
              key: AUTOMATION_DATABASE_URL
        - name: CONTROL_PLANE_API_TOKEN
          valueFrom:
            secretKeyRef:
              name: my-kis-gold-bar
              key: CONTROL_PLANE_API_TOKEN
        - name: CONTROL_PLANE_HEARTBEAT_URL
          valueFrom:
            secretKeyRef:
              name: my-kis-gold-bar
              key: CONTROL_PLANE_HEARTBEAT_URL
        - name: IDENTITY_DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: my-kis-gold-bar
              key: IDENTITY_DATABASE_URL
        - name: KIS_API_URL
          valueFrom:
            secretKeyRef:
              name: my-kis-gold-bar
              key: KIS_API_URL
        - name: KIS_APP_KEY
          valueFrom:
            secretKeyRef:
              name: my-kis-gold-bar
              key: KIS_APP_KEY
        - name: KIS_APP_SECRET
          valueFrom:
            secretKeyRef:
              name: my-kis-gold-bar
              key: KIS_APP_SECRET
        - name: KIS_TOKEN_SCOPE
          valueFrom:
            secretKeyRef:
              name: my-kis-gold-bar
              key: KIS_TOKEN_SCOPE
        - name: RABBITMQ_URL
          valueFrom:
            secretKeyRef:
              name: my-kis-gold-bar
              key: RABBITMQ_URL
        - name: REDIS_URL
          valueFrom:
            secretKeyRef:
              name: my-kis-gold-bar
              key: REDIS_URL
        readinessProbe:
          exec:
            command: ["python", "-c", "import asyncio, os, aio_pika; connection = asyncio.run(aio_pika.connect(os.environ['RABBITMQ_URL'], timeout=2)); asyncio.run(connection.close())"]
          initialDelaySeconds: 3
          periodSeconds: 5
        livenessProbe:
          exec:
            command: ["python", "-c", "import asyncio, os, aio_pika; connection = asyncio.run(aio_pika.connect(os.environ['RABBITMQ_URL'], timeout=2)); asyncio.run(connection.close())"]
          initialDelaySeconds: 10
          periodSeconds: 10
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: kis-auto-trading-nginx
  namespace: default
  labels:
    app.kubernetes.io/name: kis-auto-trading
    app.kubernetes.io/component: proxy
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: kis-auto-trading
      app.kubernetes.io/component: proxy
  template:
    metadata:
      labels:
        app.kubernetes.io/name: kis-auto-trading
        app.kubernetes.io/component: proxy
    spec:
      containers:
      - name: nginx
        image: nginx:1.27-alpine
        ports:
        - name: http
          containerPort: 80
        env:
        - name: UPSTREAM_HOST
          value: kis-auto-trading-backend
        - name: NGINX_ENVSUBST_FILTER
          value: UPSTREAM_HOST
        volumeMounts:
        - name: nginx-template
          mountPath: /etc/nginx/templates/default.conf.template
          subPath: default.conf.template
        readinessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 3
          periodSeconds: 5
        livenessProbe:
          httpGet:
            path: /health
            port: http
          initialDelaySeconds: 10
          periodSeconds: 10
      volumes:
      - name: nginx-template
        configMap:
          name: kis-auto-trading-nginx
---
apiVersion: v1
kind: Service
metadata:
  name: kis-auto-trading-load-balancer
  namespace: default
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: kis-auto-trading
    app.kubernetes.io/component: proxy
  ports:
  - name: http
    port: 8080
    targetPort: http
